* Changed: Internal only: no user-visible changes. `src/app/portfolio.rs` and `crates/keepbook-server/src/lib.rs` were each split into modules along their existing seams, and the Dioxus charts, spending view, logic, and wire DTOs were split into submodules. CLI commands, JSON output, storage format, and every public Rust path are unchanged.
