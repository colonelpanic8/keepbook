* Fixed: Made `[git].ssh_key_path` the canonical SSH identity for all libgit2 fetch, pull, push, and auto-push operations.
* Fixed: Updated Dioxus Git settings to persist selected SSH keys into the shared git configuration instead of a Git Sync-only path.
