* Fixed: Restored persisted Android SSH private keys from app-private storage even when no device-config path is available, without probing desktop home keys.
* Fixed: Fixed CI failures from new Rust 1.97 Clippy lints and Android builds exhausting hosted-runner disk space.
