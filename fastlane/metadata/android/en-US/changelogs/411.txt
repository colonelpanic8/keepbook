* Fixed: Fixed the Nix flake package version and OpenSSL inputs for clean keepbook builds from downstream flakes.
* Fixed: Simplified Marketstack single-date close fetching to avoid a Rust 1.95 release-build compiler ICE.
