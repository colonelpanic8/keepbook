* Changed: Replaced the wrapping option rows for spending Range, View, and Bucket (and the net worth graph's Range and Sampling) with a segmented-control pattern that lays options out as a single row of equal-width columns, so an option can no longer spill onto its own line on narrow screens. Segments now size their type and padding from the space actually available to them rather than the viewport.
* ...see CHANGELOG.md for the rest.
